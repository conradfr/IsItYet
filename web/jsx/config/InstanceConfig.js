'use strict';

var InstanceConfig = {
    /* store timers */
    refreshDelayWithWebsocket: 60000,
    refreshDelayWithoutWebsocket: 3000,
    /* instance page timer */
    instancePageTic: 1000
};

module.exports = InstanceConfig;
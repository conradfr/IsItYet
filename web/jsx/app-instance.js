'use strict';

var React = require('react');
var ReactDOM = require('react-dom');

var Instance = require('./components/Instance.jsx');

ReactDOM.render(
<Instance />,
    document.getElementById('instance')
);